extends ./layout.jade
block contenido 
		div(style=" width: 100% ; padding:  10px 30px 30px;  background-image: url('/public/imagenes/img/3.jpg'); ")
			h1.h1  #{filtro}  
			div(style=' padding: 25px')
			section.publicidadpagada
				for imagen in imagenes 
					if imagen.foto == 'normal'
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 600px;")
			h2.h2 Comidas 
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "comidas"
							if imagen.creator.ciudad == filtro
								div
									a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
										img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
							if imagen.creator.departamento == filtro
								div
									a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
										img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
						else
								div
									a(href="/nosotros" class="ordenado")
										img(src="/public/imagenes/img/negocio.jpg" alt="agrega tu empresa" title="Trabaja con nostros" style="max-height: 300px; max-width: 340px;  transform:traslateX (-50%) traslateY (-50%);")
			h2.h2 Compras 
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "ventas"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")

			h2.h2 Variedades
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Variedades"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Bebidas
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Bebidas"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 finca ra&iacutez
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "FincaRaiz"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Hospedaje
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Hospedaje"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Entretenimiento
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Entretenimiento"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Construcci&oacuten
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Construccion"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Dise&ntildeo
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Diseno"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Belleza
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Belleza"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Artesan&iacuteas
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Artesanias"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Recreaci&oacuten
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Recreacion"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
			h2.h2 Transporte
			section.negocios
				for imagen in imagenes 
					if imagen.foto == 'normal'
						if imagen.creator.gremio == "Transporte"
							div
								a(href="/iniciado/#{imagen.creator.username}/#{imagen.creator._id}" class="ordenado")
									img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}",class="main-img" alt="#{imagen.creator.username}" title="#{imagen.creator.username}" style="max-height: 300px; max-width: 340px;")
		div(style=' padding: 25px')
		script(type='text/javascript', src='/public/js/jquery.min.v1.js')
		script(type='text/javascript', src='/public/js/bootstrap.min.js')
		script(type='text/javascript', src='/public/slick/slick.js', charset='utf-8')
		script(type='text/javascript').
			$(document).on('ready', function() {
			$('.untiempo').slick({
			dots: true,
			infinite: true,
			speed: 900,
			slidesToShow: 1,
			adaptiveHeight: true,
			fade:true,
			autoplay: true,
			autoplaySpeed: 9500,
			});
			$('.slider-for').slick({
			slidesToShow: 1,
			slidesToScroll: 1,
			arrows: false,
			fade: true,
			asNavFor: '.slider-nav',
			centerMode: true,
			focusOnSelect: true,
			autoplay: true,
			autoplaySpeed: 4500,
			});
			$('.slider-nav').slick({
			slidesToShow: 3,
			slidesToScroll: 1,
			asNavFor: '.slider-for',
			centerMode: true,
			focusOnSelect: true
			});
			$ ( '.categorias' ) . slick ( {
			centerMode :  true ,
			centerPadding :  '60px' ,
			slidesToShow :  5,
			responsive :  [
			{
			breakpoint :  968 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow :  3
			}
			} ,
			{
			breakpoint :  681 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow :  2
			}
			}
			,
			{
			breakpoint :  431 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow :  1
			}
			}
			]
			} ) ;
			$ ( '.publicidadpagada' ) . slick ( {
			centerMode :  true ,
			centerPadding :  '60px' ,
			slidesToShow :  4,
			responsive :  [
			{
			breakpoint :  968 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow :  2
			}
			} ,
			{
			breakpoint :  681 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow : 1
			}
			}
			]
			} ) ;
			$ ( '.negocios' ) . slick ( {
			centerMode :  true ,
			centerPadding :  '60px' ,
			slidesToShow :  4,
			responsive :  [
			{
			breakpoint :  868 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow : 1
			}
			} ,
			{
			breakpoint :  520 ,
			settings :  {
			arrows :  false ,
			centerMode :  true ,
			centerPadding :  '40px' ,
			slidesToShow :  2
			}
			}
			]
			} ) ;
			});